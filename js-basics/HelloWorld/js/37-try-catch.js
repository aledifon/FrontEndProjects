'use strict'

window.addEventListener('load', () => {

    try
    {
        var year = 2019;
        alert(year);
        console.log(decodeURIComponent("https://dakgamesstudio.taplink.site/"));
    }
    catch(error)
    {
        console.log(error);
    }
});
